#include <iostream>//imput output stream

using namespace std;

int main() {

	cout << "Starting program..." << flush; //flush doesn't create a line feed or courage return

	//Indentations, Semicolons -- pay attention for it could prevent hours of searching for errors

	cout << "Hello World " << "Apple. " << "Orange. " << endl; //pronounced "c-out" the << sends the text to the output, endl tells the output to output all the text

	//c++ executes each statement in the main function one after another

	cout << "this is some more text" << endl;

	//Formatting is very important.

	return 0;

}
